name: Clear Artifact

on:
   workflow_dispatch:
jobs:
  clear:
    steps:
        - name: Checkout
          uses: actions/checkout@v4

        - uses: geekyeggo/delete-artifact@v5
          with:
              name: main